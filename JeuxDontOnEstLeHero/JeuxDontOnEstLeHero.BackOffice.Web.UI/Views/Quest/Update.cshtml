@model JeuxDontOnEstLeHero.Core.Data.Models.Quest

@{
    ViewData["Title"] = "Update";
}

<h1>Update</h1>

<h4>Quest @Model.Number </h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Update">
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="Title" class="control-label"></label>
                <input asp-for="Title" class="form-control" />
            </div>
            <div class="form-group">
                <label asp-for="Number" class="control-label"></label>
                <input asp-for="Number" class="form-control" />
            </div>
            <div class="form-group">
                <label asp-for="Description" class="control-label"></label>
                <input asp-for="Description" class="form-control" />
            </div>
            @*<div class="form-group">
            <label asp-for="Question" class="control-label"></label>
            <input asp-for="Question" class="form-control" value="@Model.Question.Title" />
        </div>
        @foreach (var question in Model.ListAnwser)
        {
            var name = "Answer" + question.Id;
            <div class="form-group">
                <label asp-for="@name" class="control-label">Question @question.Id</label>
                <input asp-for="@name" class="form-control" value="@question.Description" />
            </div>
        }*@
            <div class="form-group">
                <input type="submit" value="Update" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="List">Back to List</a>
</div>

